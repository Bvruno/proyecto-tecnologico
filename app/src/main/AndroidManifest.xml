<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TREMILY"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.tomarFotoActivity"></activity>
        <activity android:name=".activities.familia.FamiliarSeccionadoActivity" />
        <activity android:name=".activities.ActividadSeleccionadaActivity" />
        <activity android:name=".activities.perfil.EditarPerfilActivity" />
        <activity android:name=".activities.familia.NuevoFamiliarActivity" />
        <activity android:name=".activities.NuevaActividadActivity" />
        <activity android:name=".activities.familia.FamiliaActivity" />
        <activity android:name=".activities.perfil.PerfilActivity" />
        <activity android:name=".activities.RecuperarCuentaActivity" />
        <activity android:name=".activities.RegistrarActivity" />
        <activity android:name=".activities.InicioActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>